# Archived — non-winning admin overhaul prototypes

Preserved here for design-reference. The winner (`clinical-outcomes-first.html`) and the
blend source (`compliance-gauge.html`) live one level up alongside the shipped README.

- `command-centre.html` — two-pane keyboard-first admin. Rejected: optimises for web density; bwell is RN-first.
- `kpi-dashboard.html` — baseline stacked-card dashboard. Rejected: safe and mergeable, but hedges — no sharp opinion.
- `mission-control.html` — action-first queue. Rejected: misread the admin cadence (solo weekly-check-in, not daily fluent operator).
- `narrative-brief.html` — prose-first. Rejected for shipping: strongest angle-commitment but needed a new BE endpoint + LLM integration.
